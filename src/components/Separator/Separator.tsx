import { Separator as BaseUiSeparator } from '@base-ui/react'
import styles from '@/components/Separator/Separator.module.css'

export const Separator = () => {
  return (
    <BaseUiSeparator className={styles.separator} />
  )
}